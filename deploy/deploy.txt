To run as the server as daemon service on a linux system:

1. Copy face-replace.service to /etc/systemd/system

2. Run the server:

```
sudo service face-replace start
```

3. Run the client:
enter the client directory and run:

```
sudo ./runReact.sh
```

